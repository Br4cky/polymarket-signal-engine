[build]
  publish = "."

# No build command needed — static HTML + JSON
# Netlify auto-deploys on every push (including data commits from GitHub Actions)

[[headers]]
  for = "/data/*"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"
    Access-Control-Allow-Origin = "*"
